{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "bookshelf",
      "settings": {
        "client": "${process.env.REACT_APP_CLIENT || 'postgres'}",
        "database": "${process.env.REACT_APP_DATABASE || 'd80rgd499a8c8j'}",
        "host": "${process.env.REACT_APP_HOST || 'ec2-3-231-46-238.compute-1.amazonaws.com'}",
        "port": "${process.env.REACT_APP_PORT || 5432}",
        "username": "${process.env.REACT_APP_USERNAME || ''}",
        "password": "${process.env.REACT_APP_PASSWORD || ''}",
        "ssl": true
      },
      "options": {}
    }
  }
}
